---
description: Convert text to speech using macOS say command via speaker subagent
---

Use the speaker subagent to convert text to speech using the macOS `say` command.

Text to convert: $ARGUMENTS (or if no arguments, use your most recent response)

After delegating to the speaker subagent, output: 🔊
